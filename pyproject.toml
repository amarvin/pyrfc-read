[build-system]
requires = ["setuptools", "wheel", "Cython"]
build-backend = "setuptools.build_meta"

[tool.isort]
profile = "black"
skip_gitignore = true

[tool.flake8]
max-line-length = 88
exclude = """
    .git,
    .venv,
    venv,
"""
